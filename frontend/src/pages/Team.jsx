import React from 'react'

const Team = () => {
  return (
    <div><h3>Selected Users:</h3>
    <ul>
      {/* {.map((user) => (
        <li key={user.id}>
          {user.first_name} {user.last_name} ({user.domain})
        </li>
      ))} */}
    </ul>
  </div>
  )
}

export default Team